name: Build Auto-Download SDK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    name: Build Tweak
    runs-on: macos-latest

    steps:
      - name: Checkout Source
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Theos
        run: |
          bash -c "$(curl -fsSL https://raw.githubusercontent.com/theos/theos/master/bin/install-theos)"
          echo "THEOS=~/theos" >> $GITHUB_ENV

      # --- الحل: تحميل الـ SDK تلقائياً من رابط مضمون ---
      - name: Download SDK Automatically
        run: |
          mkdir -p ~/theos/sdks
          cd ~/theos/sdks
          echo "🌐 Downloading SDK..."
          # رابط مباشر لملف SDK 14.5
          curl -L -o sdk.zip https://github.com/xybp888/iOS-SDKs/releases/download/iOS-14.5/iPhoneOS14.5.sdk.zip
          
          echo "📂 Unzipping..."
          unzip -q sdk.zip
          rm sdk.zip
          echo "✅ SDK Ready!"

      - name: Prepare Dobby
        run: |
          if [ -f "Dobby" ]; then
            mv Dobby libdobby.a
            echo "✅ Dobby Prepared"
          else
            echo "⚠️ Dobby not found"
          fi

      - name: Build Package
        run: |
          export THEOS=~/theos
          # تجاهل الأخطاء وبناء الحزمة
          make package FINALPACKAGE=1 DEBUG=0 IGNORE_WARNINGS=1

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: SovereignSecurity_Auto
          path: packages/*.deb
